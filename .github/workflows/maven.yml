name: Java CI with Maven

on: [ push, pull_request ]

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        jdk-version: [25, 26]
        profile: ['modulepath', 'classpath']
    steps:
      - uses: actions/checkout@v3
      - name: 'Set up latest JDK ${{matrix.jdk-version}}'
        uses: actions/setup-java@v5
        with:
          java-version: ${{matrix.jdk-version}}
          distribution: 'zulu'
      - name: Build with Maven
        run: mvn -B package -P${{matrix.profile}} --file pom.xml
